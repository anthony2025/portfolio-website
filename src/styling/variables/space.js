import {defaultSize, defaultHeight} from './text'

const baseline = defaultSize * defaultHeight

export const xxs = baseline * 0.25 + 'px'
export const xs = baseline * 1 + 'px'
export const sm = baseline * 2 + 'px'
export const md = baseline * 3 + 'px'
export const lg = baseline * 4 + 'px'
export const xl = baseline * 5 + 'px'
